<UserControl x:Class="PromoComply.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{StaticResource DarkBackgroundBrush}" Margin="20">
        <ScrollViewer>
            <StackPanel>
                <!-- Summary Cards -->
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Documents Card -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Total Documents" Style="{StaticResource H3TextBlock}"/>
                            <TextBlock Text="{Binding TotalDocuments}" FontSize="40" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Reviewed Count Card -->
                    <Border Grid.Column="1" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Reviewed" Style="{StaticResource H3TextBlock}"/>
                            <TextBlock Text="{Binding ReviewedCount}" FontSize="40" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Flagged Count Card -->
                    <Border Grid.Column="2" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Flagged Issues" Style="{StaticResource H3TextBlock}"/>
                            <TextBlock Text="{Binding FlaggedCount}" FontSize="40" FontWeight="Bold" Foreground="{StaticResource CriticalBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Average Score Card -->
                    <Border Grid.Column="3" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Average Score" Style="{StaticResource H3TextBlock}"/>
                            <TextBlock Text="{Binding AverageScore, StringFormat='{}{0:F1}'}" FontSize="40" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Critical Issues -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Critical Issues Alert" Style="{StaticResource H3TextBlock}"/>
                        <TextBlock Text="{Binding CriticalIssuesCount, StringFormat='Found {}{0} critical compliance issues'}" Style="{StaticResource BodyTextBlock}" Foreground="{StaticResource CriticalBrush}"/>
                    </StackPanel>
                </Border>

                <!-- Recent Activity -->
                <TextBlock Text="Recent Activity" Style="{StaticResource H2TextBlock}" Margin="0,24,0,12"/>
                <Border Style="{StaticResource Card}">
                    <DataGrid ItemsSource="{Binding RecentActivity}" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Document" Binding="{Binding DocumentName}" Width="300"/>
                            <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="200"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                            <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Score Distribution -->
                <TextBlock Text="Compliance Score Distribution" Style="{StaticResource H2TextBlock}" Margin="0,24,0,12"/>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding ScoreDistribution}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Range}" Style="{StaticResource BodyTextBlock}" VerticalAlignment="Center"/>
                                        <Border Grid.Column="1" Background="{Binding Color}" CornerRadius="4" Height="24" Margin="12,0">
                                            <TextBlock Text="{Binding Count}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Grid.Column="2" Text="{Binding Count}" Style="{StaticResource BodyTextBlock}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
