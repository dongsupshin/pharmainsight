<UserControl x:Class="PromoComply.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Background="{StaticResource DarkBackgroundBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Command="{Binding ExportReportCommand}" Content="Export Report" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>

        <!-- Report Content -->
        <ScrollViewer Grid.Row="1">
            <Border Style="{StaticResource Card}" Margin="0">
                <StackPanel>
                    <TextBlock Text="Compliance Review Report" Style="{StaticResource H2TextBlock}"/>

                    <!-- Document Info -->
                    <Border Background="{StaticResource LightBackgroundBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="File Name:" FontWeight="SemiBold"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedDocument.FileName}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="File Type:" FontWeight="SemiBold" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedDocument.FileType}" Margin="0,8,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Imported Date:" FontWeight="SemiBold" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedDocument.ImportedDate, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" Margin="0,8,0,0"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Compliance Score:" FontWeight="SemiBold" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedDocument.OverallScore}" Foreground="{StaticResource AccentBrush}" FontSize="16" FontWeight="Bold" Margin="0,8,0,0"/>
                        </Grid>
                    </Border>

                    <!-- Claims Summary -->
                    <TextBlock Text="Claims Summary" Style="{StaticResource H3TextBlock}"/>
                    <Border Background="{StaticResource LightBackgroundBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Total Claims" Style="{StaticResource SecondaryTextBlock}"/>
                                <TextBlock Text="{Binding SelectedDocument.Claims.Count}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Critical Risk" Style="{StaticResource SecondaryTextBlock}"/>
                                <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource CriticalBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="High Risk" Style="{StaticResource SecondaryTextBlock}"/>
                                <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="3">
                                <TextBlock Text="Approved" Style="{StaticResource SecondaryTextBlock}"/>
                                <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Compliance Issues Summary -->
                    <TextBlock Text="Compliance Issues" Style="{StaticResource H3TextBlock}"/>
                    <Border Background="{StaticResource LightBackgroundBrush}" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                        <ItemsControl ItemsSource="{Binding SelectedDocument.ComplianceIssues}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,8">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding Title}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>

                                                <Border Grid.Column="1" CornerRadius="4" Padding="6,2">
                                                    <TextBlock Text="{Binding Severity}" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                                                </Border>
                                            </Grid>

                                            <TextBlock Text="{Binding Description}" Style="{StaticResource BodyTextBlock}" Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding Category, StringFormat='Category: {}{0}'}" Style="{StaticResource SecondaryTextBlock}"/>
                                            <TextBlock Text="{Binding Recommendation, StringFormat='Recommendation: {}{0}'}" Style="{StaticResource BodyTextBlock}" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>

                    <!-- Recommendations -->
                    <TextBlock Text="Recommendations" Style="{StaticResource H3TextBlock}"/>
                    <Border Background="{StaticResource LightBackgroundBrush}" CornerRadius="4" Padding="12">
                        <StackPanel>
                            <TextBlock Text="Address all identified compliance issues before submitting to MLR." Style="{StaticResource BodyTextBlock}" Margin="0,0,0,8"/>
                            <TextBlock Text="Ensure all claims are supported by clinical evidence and properly referenced." Style="{StaticResource BodyTextBlock}" Margin="0,0,0,8"/>
                            <TextBlock Text="Maintain fair balance between benefits and risks presentation." Style="{StaticResource BodyTextBlock}" Margin="0,0,0,8"/>
                            <TextBlock Text="Include comprehensive Important Safety Information section." Style="{StaticResource BodyTextBlock}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
