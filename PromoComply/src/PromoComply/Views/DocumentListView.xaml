<UserControl x:Class="PromoComply.Views.DocumentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PromoComply.Converters"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <local:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <local:ScoreToColorConverter x:Key="ScoreToColorConverter"/>
    </UserControl.Resources>
    <Grid Background="{StaticResource DarkBackgroundBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Command="{Binding ImportDocumentCommand}" Content="Import Document" Style="{StaticResource PrimaryButton}"/>
            <Button Command="{Binding AnalyzeDocumentCommand}" Content="Analyze" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
            <Button Command="{Binding ExportReportCommand}" Content="Export Report" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
            <Button Command="{Binding RemoveDocumentCommand}" Content="Remove" Style="{StaticResource DangerButton}" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Document List -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0">
            <DataGrid ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}" IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="250"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding FileType}" Width="80"/>
                    <DataGridTextColumn Header="Imported Date" Binding="{Binding ImportedDate, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="160"/>
                    <DataGridTemplateColumn Header="Status" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}" CornerRadius="4" Padding="8,4" Margin="4">
                                    <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Score" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding OverallScore}" Foreground="{Binding OverallScore, Converter={StaticResource ScoreToColorConverter}}" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Claims" Binding="{Binding Claims.Count}" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Issues" Binding="{Binding ComplianceIssues.Count}" Width="80" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
