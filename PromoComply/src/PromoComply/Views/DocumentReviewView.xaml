<UserControl x:Class="PromoComply.Views.DocumentReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PromoComply.Converters"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:SeverityToColorConverter x:Key="SeverityToColorConverter"/>
    </UserControl.Resources>
    <Grid Background="{StaticResource DarkBackgroundBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Document.FileName}" Style="{StaticResource H2TextBlock}"/>
                    <TextBlock Text="{Binding Document.FileType, StringFormat='File Type: {}{0}'}" Style="{StaticResource SecondaryTextBlock}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="20,0,20,0">
                    <TextBlock Text="Compliance Score" Style="{StaticResource BodyTextBlock}"/>
                    <TextBlock Text="{Binding ComplianceScore}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Claims / Issues" Style="{StaticResource BodyTextBlock}"/>
                    <TextBlock Text="{Binding Claims.Count, StringFormat='{}{0} Claims'}" Style="{StaticResource BodyTextBlock}"/>
                    <TextBlock Text="{Binding ComplianceIssues.Count, StringFormat='{}{0} Issues'}" Style="{StaticResource BodyTextBlock}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Claims Panel -->
            <Grid Grid.Column="0" Margin="0,0,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Detected Claims" Style="{StaticResource H3TextBlock}"/>

                <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ListBox Grid.Row="0" ItemsSource="{Binding Claims}" SelectedItem="{Binding SelectedClaim}" Background="{StaticResource LightBackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="8">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding Text}" Style="{StaticResource BodyTextBlock}" TextWrapping="Wrap"/>

                                                <Border Grid.Column="1" Background="{Binding RiskLevel}" CornerRadius="4" Padding="6,2" Margin="8,0,0,0">
                                                    <TextBlock Text="{Binding RiskLevel}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                                </Border>
                                            </Grid>

                                            <TextBlock Text="{Binding ClaimType, StringFormat='Type: {}{0}'}" Style="{StaticResource SecondaryTextBlock}"/>
                                            <TextBlock Text="{Binding Location, StringFormat='Location: {}{0}'}" Style="{StaticResource SecondaryTextBlock}"/>
                                            <TextBlock Text="{Binding HasReference, StringFormat='Reference: {}{0}'}" Style="{StaticResource SecondaryTextBlock}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Claim Actions -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                            <Button Command="{Binding ApproveClaim}" Content="Approve" Style="{StaticResource PrimaryButton}"/>
                            <Button Command="{Binding FlagClaim}" Content="Flag" Style="{StaticResource DangerButton}" Margin="8,0,0,0"/>
                            <Button Command="{Binding RejectClaim}" Content="Review" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Compliance Issues Panel -->
            <Grid Grid.Column="1" Margin="8,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Compliance Issues" Style="{StaticResource H3TextBlock}"/>

                <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ListBox Grid.Row="0" ItemsSource="{Binding ComplianceIssues}" SelectedItem="{Binding SelectedIssue}" Background="{StaticResource LightBackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="8">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding Title}" Style="{StaticResource H3TextBlock}" Margin="0,0,0,0"/>

                                                <Border Grid.Column="1" Background="{Binding Severity, Converter={StaticResource SeverityToColorConverter}}" CornerRadius="4" Padding="6,2">
                                                    <TextBlock Text="{Binding Severity}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                                </Border>
                                            </Grid>

                                            <TextBlock Text="{Binding Description}" Style="{StaticResource BodyTextBlock}" Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding Recommendation, StringFormat='Recommendation: {}{0}'}" Style="{StaticResource SecondaryTextBlock}"/>
                                            <TextBlock Text="{Binding IsResolved, StringFormat='Status: {}{0}'}" Style="{StaticResource SecondaryTextBlock}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Issue Actions -->
                        <Button Grid.Row="1" Command="{Binding ResolveIssue}" Content="Toggle Resolution" Style="{StaticResource PrimaryButton}" Margin="0,8,0,0"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
